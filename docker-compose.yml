services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file: .env
    environment:
      DATABASE_URL: sqlite:////data/rbac.db
    volumes:
      - rbac-data:/data
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000

volumes:
  rbac-data:
